#include <stdio.h>
#include <stdlib.h>
// operado de resto de divisao usando o % exemplo de 20 / 7 o resto e 6 ou 21 / 2 que o resto seria 1.
int main () {
    int a = 10, b = 20;
    printf("resto da divisao inteira: %d\n", b % a);
    return 0;
}